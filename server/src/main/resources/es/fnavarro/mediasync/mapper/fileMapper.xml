<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="es.fnavarro.mediasync.mappers.FileMapper">

	<select id="list" parameterType="map" resultType="File">
		SELECT * FROM FILES
			WHERE SAVED = 0 OR DOWNLOADED = 0
	</select>
	
	<insert id="add" parameterType="map">
		INSERT INTO FILES (NAME, PATH, SIZE, SAVED, DOWNLOADED, DATE_CREATED)
		   VALUES(#{file.name}, #{file.path} , #{file.path}, 0, 0, CURRENT_TIMESTAMP)
	</insert>

</mapper>